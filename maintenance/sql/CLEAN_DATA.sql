-- =============================================================================
-- سكربت تنظيف البيانات (CLEAN DATA) - Hader System
-- =============================================================================
-- هذا السكربت يقوم بمسح جميع بيانات الحضور، الرسائل، والسجلات
-- ولكنه يحتفظ ببيانات الطلاب، الفصول، والمستخدمين.
-- استخدم هذا السكربت عند بداية تيرم جديد أو لتشغيل النظام بشكل نظيف.
-- =============================================================================

BEGIN;

-- 1. مسح سجلات الحضور (Attendance)
TRUNCATE TABLE attendance_logs CASCADE;

-- 2. مسح سجلات الاستئذان (Exits)
TRUNCATE TABLE exits CASCADE;

-- 3. مسح الإشعارات (Notifications)
TRUNCATE TABLE notifications CASCADE;

-- 4. مسح سجلات النظام والقياس عن بعد (Telemetry & Logs)
-- ملاحظة: نستخدم DELETE بدلاً من TRUNCATE هنا لتجنب مشاكل الصلاحيات إن وجدت
-- ونمسح فقط الجداول التي تم التأكد من وجودها في التحديثات الأخيرة.

-- مسح سجلات التدقيق (Auth Audit)
DELETE FROM auth_audit_logs;

-- مسح سجلات أخطاء المتصفح (Client Errors)
DELETE FROM client_error_logs;

COMMIT;

-- =============================================================================
-- تم مسح البيانات بنجاح. النظام جاهز للتشغيل.
-- =============================================================================
