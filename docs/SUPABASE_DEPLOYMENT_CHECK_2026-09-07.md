# تحقق Supabase بعد إفادة تطبيق الترحيلات — 7 سبتمبر 2026

## تحديث: تحديد المشروع وضبط البيئة

حدد المستخدم المشروع `ifbvppkeljgikoztwpak` صراحة. أُنشئ `.env.local` للتطبيق الرئيسي بعنوان `https://ifbvppkeljgikoztwpak.supabase.co` ومفتاح `anon` المطابق المستخرج من حزمة التطبيق المنشورة. تم التحقق من أن المفتاح يحمل دور `anon` ومرجع المشروع الصحيح، دون إظهاره في السجل. الملف مستبعد من Git وصلاحياته `600`.

الوضع المحلي المضبوط هو `cloud`، مع `VITE_ALLOW_LOCAL_FALLBACK=false` و`VITE_ENABLE_BOOTSTRAP_ADMIN=false`. أكّد `loadEnv` الخاص بـVite قراءة هذه القيم، ونجح بناء الإنتاج ووجود عنوان المشروع والمفتاح المطابق في الحزمة الناتجة.

نجحت خدمة المصادقة `/auth/v1/health` بالرمز 200، وكذلك طلب `/rest/v1/students?select=id&limit=0` بالرمز 200 دون إرجاع بيانات طلاب. واجهة وصف المخطط `/rest/v1/` رفضت مفتاح `anon` لأنها تطلب `service_role`؛ لم يُستبدل المفتاح بمفتاح إداري ولم تُستدعَ دالة الغياب.

أظهر فحص قيم Vercel الحالية في مجلد معزول، دون أي `.env.local`، أن قيم `VITE_SUPABASE_URL` و`VITE_SUPABASE_ANON_KEY` فارغة في الإنتاج والمعاينة. أكد سحب إعدادات الإنتاج إلى ملف مؤقت محمي النتيجة؛ حُذف الملف بعد الفحص. هذا يختلف عن الحزمة المنشورة سابقًا التي تحتوي اتصالًا بالمشروع المطلوب.

رُفضت محاولة تحديث خمسة متغيرات Vercel بواسطة مراجعة الموافقات الآلية قبل تنفيذها، لأن النطاق شمل الإنتاج ووضع التشغيل والحماية. لم تُعدّل بيئات Vercel ولم يُنشر التطبيق. التحديث المقترح للموافقة الآن يقتصر على متغيرين في Production وPreview لمشروع Vercel `hader-saud-2027`:

| المتغير | القيمة المطلوبة |
|---|---|
| `VITE_SUPABASE_URL` | `https://ifbvppkeljgikoztwpak.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | مفتاح `anon` المطابق الذي نجح معه اختبار القراءة؛ لا تُعرض قيمته هنا |

هذا التحديث المقترح لا يشمل تغيير وضع التشغيل أو الحماية أو إعادة النشر. التحقق السابق أدناه يخص مشروع HADER2027 المختلف، ولا يثبت غياب الترحيل في المشروع الذي اختاره المستخدم الآن.

## الفحص السابق قبل تحديد المشروع

أفاد المستخدم بأنه طبّق الترحيلات على Supabase الحالي. أُجري تحقق للقراءة فقط عبر جلسة لوحة Supabase الحالية وفحص ملفات JavaScript العامة للموقع المنشور. لم يُنفّذ أي ترحيل أو تسجيل غياب أو تعديل لبيانات الطلاب.

## النتائج المؤكدة

| المصدر | المشروع/النتيجة |
|---|---|
| `supabase/.temp/project-ref` المحلي | `ifbvppkeljgikoztwpak` |
| حزمة الموقع `https://hader-saud-2027.vercel.app/assets/index-yPq_fUeU.js` وقت الفحص | تحتوي عنوان `https://ifbvppkeljgikoztwpak.supabase.co` |
| المشروع الظاهر في مؤسسة HimArt-1 الحالية | HADER2027 — `uqvkffyukzsxefhkfmlq` |
| `public.mark_hader_automatic_absence()` في HADER2027 | غير موجودة بالاسم والتوقيع اللذين يستدعيهما التطبيق |
| جداول `students` و`attendance_logs` و`settings` في HADER2027 | موجودة |
| فهرس فريد صالح غير مشروط على `(student_id, date)` | موجود |
| `attendance_logs` في منشور `supabase_realtime` | موجود |

الوصول إلى رابط المشروع المحلي القديم عبر جلسة لوحة التحكم أعاد التوجيه إلى قائمة المشاريع؛ هذا لا يثبت وحده أن المشروع محذوف. لم يتوفر تسجيل دخول صالح لـSupabase CLI، ولم يوجد ملف إعدادات محلي فعلي للتطبيق الرئيسي.

استعلام التحقق المستخدم بدأ بـ`BEGIN READ ONLY` واقتصر على كتالوج PostgreSQL ومنشورات Realtime، ثم `COMMIT`. لم يستدعِ دالة الغياب. يمكن مراجعة نتيجته في [لوحة SQL للمشروع الذي فُحص](https://supabase.com/dashboard/project/uqvkffyukzsxefhkfmlq/sql/ad1c432b-745f-4f2d-a1f6-e89f623829cf).

## الاستنتاج والخطوة المطلوبة

توجد وجهتان مختلفتان بين التطبيق المنشور والمشروع الظاهر في الحساب. يلزم تحديد المشروع الذي نُفّذ عليه الترحيل والمشروع المقصود لتشغيل التطبيق، قبل تغيير الاتصال أو تنفيذ ترحيلات إضافية.

إذا كان HADER2027 هو المقصود، فالدالة الجديدة لم تظهر فيه وقت الفحص. الملف المحلي المطلوب هو `supabase/migrations/20260907090000_atomic_automatic_absence.sql`. بصمة SHA-256 للملف المفحوص:

```text
249482502fa4ac01e272c795ec79085ec2587a43040c96b04b48e9203a8ed622
```

إذا كان المشروع القديم هو المقصود، فلا تعني نتائج HADER2027 شيئًا عن اكتمال ترحيلاته؛ يلزم الوصول إلى المشروع الصحيح والتحقق منه. لم تُغيّر مراجع المشاريع أو إعدادات Vercel ولم يُنشر التطبيق أثناء هذه الجولة.
